import React from 'react';
import { shallow, mount, render } from 'enzyme';
import ShowTask from './ShowTask';

describe('ShowTask', () => {
  let wrapper;
  beforeEach(() => wrapper = shallow(<ShowTask />));

});